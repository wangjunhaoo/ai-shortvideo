2026-04-02：项目配置弹窗的视觉风格下拉实际传入了 5 个 ART_STYLES 选项，但 UI 只露出 4 个。根因不是数据缺失，而是 `glass-surface-soft` 使用 `backdrop-filter` 形成 stacking context，导致第一张卡片内的 `StyleSelector` 下拉第三行被后续模型卡片覆盖。修复：1）在 `src/components/ui/config-modals/ConfigEditModal.tsx` 给视觉风格卡片增加 `relative z-10`，抬高整个卡片层级；2）在 `src/components/ui/config-modals/config-modal-selectors.tsx` 给样式下拉面板增加 `max-h-60 overflow-y-auto custom-scrollbar` 和 `minWidth: 280px`，避免未来风格继续增加时再次截断；3）新增 `tests/unit/components/config-edit-modal.test.ts`，固定 5 个视觉风格常量和视觉风格卡片层级约束。