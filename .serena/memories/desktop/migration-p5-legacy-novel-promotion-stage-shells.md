2026-03-27：将旧 src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components 下 14 个阶段级入口组件压成 renderer compat shell，包括 AssetLibrary、AssetsStage、ConfigStage、NovelInputStage、PanelEditForm、PromptsStage、ScriptStage、ScriptView、SmartImportWizard、StoryboardStage、VideoStage、VideoStageRoute、VoiceStage、VoiceStageRoute。为兼容新 renderer API，旧 storyboard/index.tsx 已补 CharacterPickerModal / LocationPickerModal 的 labels，旧 storyboard 文件中的 PanelEditData 导入已改成 import type。新增 scripts/guards/legacy-novel-promotion-stage-shell-guard.mjs，并接入 package.json 的 check:legacy-novel-promotion-stage-shell 与 test:guards。相关 legacy guards 与 desktop:build:web 已通过。