2026-03-22：继续推进 renderer/novel-promotion 的 assets 区对称重构。`LocationCard.tsx` 已新增 `location-card/useLocationCardState.ts`、`LocationCardSelectionView.tsx`、`LocationCardCompactView.tsx`，当前主要承担状态 hook 装配、动作节点拼装和 selection/compact 模式分发。结合上一轮的 `LocationSectionHeader.tsx` 与 `LocationGridItem.tsx`，场景区已经与角色区形成一致的层次：Section -> Header / 列表项 -> Card -> state hook + mode views。`npm run desktop:build:web` 已通过。后续建议回到 `ScriptViewRuntime.tsx` 继续压薄 script-view 装配层，或继续拆 stage 组件。