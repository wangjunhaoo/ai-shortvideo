2026-03-22：开始推进 packages/renderer。已新增 packages/renderer/pages，并在 tsconfig.json 中加入 @renderer/* 路径别名。profile/page.tsx、workspace/page.tsx、workspace/asset-hub/page.tsx、workspace/[projectId]/page.tsx 已改成薄入口，分别转发到 packages/renderer/pages/profile-page.tsx、workspace-page.tsx、asset-hub-page.tsx、project-detail-page.tsx。迁移策略是整页搬迁、保持行为不变，仅把原页面中的相对导入改成绝对导入。desktop:build:web 已通过，说明 renderer 第一批入口迁移与当前 Next 外壳兼容。后续应继续把页面内部组件、hooks、查询逻辑逐步下沉到 packages/renderer。