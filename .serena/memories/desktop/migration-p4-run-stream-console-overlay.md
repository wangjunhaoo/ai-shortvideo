2026-03-22：继续推进 renderer/novel-promotion 的重容器抽象。新增 `run-stream-console/types.ts` 收口 `RunStreamState`/`RunStreamStep`，新增 `RunStreamConsoleOverlay.tsx` 统一承接最小化徽标、fallback stage 构建、active/selected 状态计算、重试触发和 overlay 卡片渲染。`WorkspaceRunStreamConsoles.tsx` 已把 story-to-script 与 script-to-storyboard 两套近乎重复的渲染压缩成两次 `RunStreamConsoleOverlay` 实例化。`npm run desktop:build:web` 已通过。后续可继续找 `script-view` 或 `storyboard` 下仍偏重的装配层，或继续清理 `novel-promotion` 目录里的重复类型与壳层。