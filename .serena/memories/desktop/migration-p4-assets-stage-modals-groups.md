2026-03-22：继续推进 renderer/novel-promotion 的 modal 装配层拆分。新增 `AssetsStageModalTypes.ts` 统一承接 `editingAppearance / editingLocation / imageEditModal / characterImageEditModal / voiceDesignCharacter / editingProfile / copyFromGlobalTarget` 等状态类型；新增 `AssetsEditingModals.tsx` 和 `AssetsSupportModals.tsx`，分别承接编辑/创建类模态框与音色/档案/全局复制类模态框。`AssetsStageModals.tsx` 当前只负责两组 modal 的分发，不再直接承载全部 JSX。`npm run desktop:build:web` 已通过。后续可继续清理 `WorkspaceHeaderShell` 周边的重复类型，或继续找其他偏重 runtime/container 做同类拆分。