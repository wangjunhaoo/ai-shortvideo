2026-03-22：继续推进 renderer 内部模块化，在 `packages/renderer/modules/workspace` 新增 `useWorkspacePageState.ts`，把 `workspace-page` 内的状态与副作用整体下沉。hook 已收口 `useSession` 登录态守卫、项目列表请求、分页/搜索联动、新建项目后的模型配置检测、编辑项目、删除确认、表单重置与搜索清空。`packages/renderer/pages/workspace-page.tsx` 现只保留 `useTranslations` 和页面组件拼装，通过 `useWorkspacePageState(t)` 获取全部状态与动作。为解决 `next-intl` translator 类型约束，`packages/renderer/modules/workspace/types.ts` 补充了 `TranslationValues` 与 `TranslationFn`。`npm run desktop:build:web` 已通过。workspace 页面现在已经具备“页面编排层 + workspace hook + workspace 展示模块”的结构，下一步适合转去处理 `project-detail-page` 的页面内状态与更深的 novel-promotion 私有依赖。