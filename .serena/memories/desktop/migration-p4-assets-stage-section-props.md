2026-03-22：继续推进 renderer/novel-promotion 的 AssetsStage 容器瘦身。新增 `packages/renderer/modules/project-detail/novel-promotion/components/assets/hooks/useAssetsStageSectionProps.ts`，把 `AssetsStage.tsx` 中 `statusOverlayProps / toolbarProps / unconfirmedProfilesProps / characterSectionProps / locationSectionProps / modalsProps` 六组子组件 props 的组合逻辑整体移交给新 hook，`AssetsStage.tsx` 当前更接近项目资产订阅 + 业务 hook 装配 + `AssetsStageSections` 分发。期间修正了 `handleConfirmProfile` 在 `UnconfirmedProfilesSection` 与 `AssetsStageModals` 间的签名差异，使 `AssetsStageModals` 继续获得完整的 `Promise<void>` 回调契约。`npm run desktop:build:web` 已通过。后续可继续清理 `WorkspaceHeaderShell` 周边重复类型，或继续找更重的 runtime/container 做同类拆分。