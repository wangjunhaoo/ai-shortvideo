2026-03-22：继续推进 novel-promotion 轻量资产编辑路由薄化。packages/engine/services/novel-promotion-asset-service.ts 新增 cleanupNovelPromotionUnselectedImages、updateNovelPromotionAssetLabel、uploadNovelPromotionAssetImage，以及内部辅助 updateAssetImageLabel / createUploadedLabeledImage。对应地，src/app/api/novel-promotion/[projectId]/cleanup-unselected-images/route.ts、update-asset-label/route.ts、upload-asset-image/route.ts 已收缩为薄路由，仅保留鉴权、参数解析与 NextResponse 包装。同时，update-appearance/route.ts 与 update-location/route.ts 也已改为复用现有 asset service，不再在 route 内直接维护描述 JSON 或场景描述写回。真实桌面回归使用 256x256 测试图，完成角色/场景图片上传、选择保留图、cleanup-unselected-images 清理未选中图、update-asset-label 重新打标签，并通过开发态 Electron SQLite（C:/Users/pc/AppData/Roaming/Electron/data/waoowaoo.db）直接回查确认：角色与场景都只保留选中图，索引收口为 0，重新打标签后 image key 已更新。首次回归失败原因是最初使用 1x1 PNG，导致标签高度计算为 0，sharp 报 Expected valid width, height and channels to create a new input image；改用 256x256 测试图后通过。当前 update-appearance / update-location 已完成薄路由改造，但尚未独立补回归。