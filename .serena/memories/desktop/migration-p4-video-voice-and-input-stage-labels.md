2026-03-25：继续推进 renderer 主链的显式 labels 收口。已为 VideoTimelinePanel 增加 VideoTimelinePanelLabels，并由 useVideoStageRuntime.tsx 注入。已将 VoiceToolbar、EmbeddedVoiceToolbar、SpeakerVoiceStatus、VoiceLineCard、EmptyVoiceState、EmotionSettingsPanel、SpeakerVoiceBindingDialog 改为显式 labels，分别通过 VoiceControlPanelLabels、VoiceLineListLabels、SpeakerVoiceBindingDialogLabels 由 useVoiceStageRuntime.tsx 统一组装。结果：video-stage、voice、voice-stage 三个目录下除 useVideoStageRuntime.tsx 与 useVoiceStageRuntime.tsx 外，展示组件的 useTranslations 已清零。随后将 CurrentEpisodeBanner、NovelTextInputPanel、NovelInputConfigPanel、NovelInputActionPanel、RatioSelector、StyleSelector 改为显式 labels，由 NovelInputStage.tsx 统一组装；input-stage 目录内的 useTranslations 已清零，仅 NovelInputStage.tsx 作为根翻译边界保留 useTranslations。desktop:build:web 已通过。