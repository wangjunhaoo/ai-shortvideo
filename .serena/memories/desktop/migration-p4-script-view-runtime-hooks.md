2026-03-22：继续推进 renderer/novel-promotion 的 script-view 装配层收口。已新增 `components/script-view/types.ts`、`useScriptViewTranslations.ts`、`useScriptViewClipSaving.ts`、`useScriptViewClipAssetState.ts`。`ScriptViewRuntime.tsx` 已移除内联的 translator 包装、savingClips 状态与 handleClipUpdateWithSaving、assetViewMode/selectedClipId/activeCharIds/activeLocationIds/selectedAppearanceKeys/handleUpdateClipAssets/global missing assets 等状态与派生逻辑，当前主要保留 `useProjectAssets(projectId)` 查询、assetsLoadingState 计算和 `ScriptViewScriptPanel` / `ScriptViewAssetsPanel` 编排。`npm run desktop:build:web` 已通过。后续可继续拆 stage 组件或更重的装配层。