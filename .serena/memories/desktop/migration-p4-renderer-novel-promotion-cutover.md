2026-03-22：`novel-promotion` 模式树已切到 `packages/renderer/modules/project-detail/novel-promotion`，并完成守卫/审计脚本路径同步。已更新 `scripts/check-outbound-image-unification.ts`、`scripts/guards/no-server-mirror-state.mjs`、`scripts/guards/no-multiple-sources-of-truth.mjs`、`scripts/guards/task-target-states-no-polling-guard.mjs`、`scripts/guards/task-status-cutover-audit.sh`，把旧 `src/app/[locale]/workspace/[projectId]/modes/novel-promotion` 路径切到新 renderer 目录。顺手收掉两处 guard 假阳性：1）`check-outbound-image-unification` 对 `asset-hub modify-image` 的 sanitize 校验已改为检查 `packages/engine/services/asset-hub-task-service.ts`，并把 `VideoPanelCardHeader` 的规则改成匹配当前 renderer 结构；2）`task-target-states-no-polling-guard` 不再要求 `useTaskTargetStateMap` 静态 `refetchInterval: false`，改为要求只在 `queued/processing` 期间条件轮询。验证已通过：`npm run desktop:build:web`、5 个守卫脚本，以及 `rg -n -F -- "src/app/[locale]/workspace/[projectId]/modes/novel-promotion" src tests scripts` 清零。后续应继续下沉 `workspace-page` 模块，并开始清理 `src/app` 下被 renderer/modules 替代的旧页面私有实现。