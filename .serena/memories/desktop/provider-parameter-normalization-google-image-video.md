2026-03-28：针对桌面端图片/视频 provider INVALID_ARGUMENT 问题，统一采用“运行时参数规范化”而不是模型 alias。关键规则：1) Google / gemini-compatible 的 gemini-3.1-flash-image-preview 若收到历史 resolution=0.5K，统一规范为 1K；同时恢复该模型在 user-api-config 与前端 preset 中的候选，不再替换为 gemini-2.5-flash-image。2) Google Veo 3.1 / 3.1 Fast 若为 firstlastframe，或携带 lastFrameImageUrl，或 resolution 为 1080p/4k，则 duration 运行时强制规范为 8 秒。规范化同时落在 packages/core/model-runtime-aliases.ts、packages/engine/config-service.ts（含 capabilityDefaults / capabilityOverrides / runtimeSelections / resolved options）、以及具体生成器 src/lib/generators/image/google.ts、src/lib/generators/image/gemini-compatible.ts、src/lib/generators/video/google.ts、src/lib/gemini-batch-utils.ts，避免 UI、计费、worker、provider 请求之间再次出现值域漂移。图片 capability/pricing catalog 也已移除 gemini-3.1-flash-image-preview 的 0.5K 选项，最小分辨率统一为 1K。