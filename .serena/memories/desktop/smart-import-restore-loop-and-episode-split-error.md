2026-03-30：修复 smart-import 剧本分析失败后循环与英文 Boundary Constraints 泄漏问题。packages/renderer/modules/project-detail/novel-promotion/components/smart-import/hooks/useWizardState.ts 不再因 importStatus=pending 直接进入 preview，也不再在 episodes 为空时依赖 effect+mutation 对象反复触发恢复；新增 hasAttemptedRestoreRef，只在每次项目进入 pending 恢复窗口时尝试加载一次已保存剧集，恢复成功才切到 preview，空结果或异常则回到 select，避免失败后无限请求/循环。src/lib/workers/handlers/episode-split.ts 新增 normalizeEpisodeSplitError，把 startMarker/endMarker 缺失、无法定位、偏差过大、Boundary Constraints、invalid episodes payload/Expected JSON object 等边界或格式错误统一收敛为中文友好提示，避免将内部 prompt 片段直接透传到前端。tests/unit/worker/episode-split.test.ts 已改为 mock @engine/runtime-context 当前仓储注入链，并新增边界失败错误文案覆盖；npx vitest run tests/unit/worker/episode-split.test.ts 与 npx tsc --noEmit --pretty false --incremental false 均通过。