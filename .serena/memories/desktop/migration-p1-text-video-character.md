2026-03-21：P1 继续推进，已完成 video.worker、text.worker、character-profile 链的 repository 收口。video.worker 已通过 createTaskExecutionContext(job) + repositories.project 读取 panel/voiceLine 并写回 video/lipSync 结果，不再在 worker 业务层直接依赖 prisma。project-repository 新增/扩展了 getPanelById、getPanelByStoryboardIndex、getVoiceLineById、updatePanelVideo、updatePanelLipSyncVideo。text.worker 已将 regenerate_storyboard_text 与 insert_panel 两条链切到 repositories.project，并新增 getStoryboardForTextRegeneration、getStoryboardAssets、replaceStoryboardPanels、getStoryboardForInsertPanel、insertPanelAfter。character-profile-helpers.ts 与 character-profile.ts 已改为通过 ProjectRepository 读取项目分析模型上下文、角色档案目标和待确认角色，并通过仓储写入 profileData、appearance、profileConfirmed。相关改动已执行 npx tsc --noEmit --pretty false --incremental false 并通过。剩余主要 prisma 直连集中在图像链：asset-hub-image-task-handler、asset-hub-modify-task-handler、character-image-task-handler、image-task-handler-shared、image-task-handlers-core、panel-image-task-handler、location-image-task-handler、reference-to-character、shot-ai-persist、shot-ai-variants、panel-variant-task-handler。