2026-03-25：继续推进 video-stage / voice-stage 的显式 labels 收口。已为 VideoTimelinePanel 增加 VideoTimelinePanelLabels，并在 useVideoStageRuntime.tsx 统一注入 title / linesCount / audioGeneratedCount。已为 VoiceToolbar、EmbeddedVoiceToolbar、SpeakerVoiceStatus 增加对应 labels 接口，并通过 VoiceControlPanelLabels 在 useVoiceStageRuntime.tsx 中统一组装 toolbar / embeddedToolbar / speakerStatus 文案。已进一步把 VoiceLineCard、EmptyVoiceState、EmotionSettingsPanel 收口成 VoiceLineListLabels，由 useVoiceStageRuntime.tsx 注入。最后将 SpeakerVoiceBindingDialog 改成显式 SpeakerVoiceBindingDialogLabels，由 useVoiceStageRuntime.tsx 注入 inlineBinding 文案。结果：packages/renderer/modules/project-detail/novel-promotion/components/video-stage、voice、voice-stage 三个目录下，除 useVideoStageRuntime.tsx 与 useVoiceStageRuntime.tsx 两个根 runtime 外，展示组件的 useTranslations 已清零。desktop:build:web 已通过。