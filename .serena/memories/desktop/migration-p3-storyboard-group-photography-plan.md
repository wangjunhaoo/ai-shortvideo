2026-03-22：继续推进 novel-promotion 编辑态路由薄化。packages/engine/services/novel-promotion-editing-service.ts 新增 createNovelPromotionStoryboardGroup、moveNovelPromotionStoryboardGroup、deleteNovelPromotionStoryboardGroup、updateNovelPromotionPhotographyPlan。src/app/api/novel-promotion/[projectId]/storyboard-group/route.ts 的 POST/PUT/DELETE 与 photography-plan/route.ts 的 PUT 已改成薄路由，只保留 requireProjectAuthLight、参数解析和 NextResponse 包装。此轮真实桌面回归直接通过 API 完成注册、登录、创建项目与剧集，然后创建两组 storyboard group、执行上移、写入 photographyPlan、删除一组分镜，再连接开发态 Electron SQLite（C:/Users/pc/AppData/Roaming/Electron/data/waoowaoo.db）回查，确认顺序交换、摄影方案按现有契约 JSON.stringify 落库、删除后仅剩目标 storyboard 且初始 panel 正常保留。构建校验顺序调整为先 npm run desktop:build:web，再执行 npx tsc --noEmit --pretty false --incremental false，以避免 .next/types 缺失造成的假失败。