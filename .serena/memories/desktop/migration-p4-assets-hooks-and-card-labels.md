2026-03-25：renderer/novel-promotion 的 assets 主链继续收口。UnconfirmedProfilesSection 和 AssetsSupportModals 已改成纯分发层，CharacterProfileCard/CharacterProfileDialog 的 labels 提升到 useAssetsStageSectionProps 统一组装。AssetsEditingModals 的 ImageEditModal labels 也已提升到 useAssetsStageSectionProps。随后继续把 CharacterCard / LocationCard 的翻译边界提升到 CharacterGroupPanel / LocationGridItem，再进一步提升到 CharacterSection / LocationSection / useAssetsStageSectionProps。当前 assets/hooks、assets/character-card/*、assets/location-card/*、CharacterCard、LocationCard、CharacterGroupPanel、LocationGridItem、UnconfirmedProfilesSection、AssetsSupportModals、AssetsEditingModals 都已清掉 useTranslations。assets 目录下剩余的直接翻译热点只剩一个未被 renderer 主链引用的 AddLocationModal.tsx，可视为残留死代码候选；删除需用户确认。所有本轮改动已通过 npx tsc --noEmit --pretty false --incremental false。