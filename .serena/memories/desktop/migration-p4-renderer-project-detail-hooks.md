2026-03-22：继续推进 renderer，`project-detail-page` 已开始从页面级大组件向模块化 hook/组件拆分。新增 `packages/renderer/modules/project-detail/detail-types.ts`，统一沉淀 `Stage`、`VALID_STAGES`、`Episode`、`NovelPromotionData` 与翻译函数类型。新增 `useProjectDetailRouteState.ts`，收口 `stage/episode` URL 参数解析、`effectiveStage` 归一化、剧集排序与 `selectedEpisodeId`、`importStatus/shouldShowImportWizard`、URL 回写和剧集切换。新增 `useProjectDetailModelSetup.ts`，收口导入向导前的默认分析模型检测、模型草稿、模型配置弹窗开关和保存逻辑。新增 `ProjectDetailImportGate.tsx`，承接 loading 卡片、模型未配置 gate、模型配置弹窗和 SmartImportWizard 入口。新增 `useProjectDetailEpisodeActions.ts`，收口创建剧集、智能导入完成后的刷新与跳转、重命名剧集、删除剧集。`packages/renderer/pages/project-detail-page.tsx` 已改为装配层，保留 project/episode 查询、初始化/错误态和 `NovelPromotionWorkspace` 组装。构建已通过。下一步适合继续拆 project-detail 剩余 query 装配和 novel-promotion 模式树内部的大块页面私有逻辑。