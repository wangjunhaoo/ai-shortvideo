2026-03-23：继续瘦身 renderer storyboard 的 ImageSectionCandidateMode.tsx。新增 ImageSectionCandidateMode.types.ts，统一收口候选图模式、缩略图区、底部动作区和状态角标的 props 契约；新增 hooks/useImageSectionCandidateModeState.ts，把候选过滤、选中索引、确认中状态和确认动作从主组件中抽出；新增 ImageSectionCandidateThumbnails.tsx、ImageSectionCandidateActions.tsx、ImageSectionCandidateStatusBadge.tsx，分别承接缩略图列表、底部动作区和顶部状态角标。这样 ImageSectionCandidateMode.tsx 现在主要负责主图预览和三段子视图分发。desktop:build:web 已通过。