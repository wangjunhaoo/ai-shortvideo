2026-03-22：继续推进 renderer/novel-promotion 的 script-view 模块化。`ScriptViewScriptPanel.tsx` 已拆出 `EditableText.tsx`、`screenplay.ts`、`ScreenplaySceneCard.tsx`。其中 `screenplay.ts` 集中承接 `ScreenplayData/ScreenplayScene` 类型、`parseScreenplay` 与 clone helper；`ScreenplaySceneCard.tsx` 承接 scene 头信息、描述和 content 流的渲染与保存；`EditableText.tsx` 承接局部编辑状态。`ScriptViewScriptPanel.tsx` 当前主要保留 clips 列表编排、saving 状态、选中态和 summary/content 兜底。`npm run desktop:build:web` 已通过。后续可继续拆 assets 大组件或 ScriptViewRuntime。