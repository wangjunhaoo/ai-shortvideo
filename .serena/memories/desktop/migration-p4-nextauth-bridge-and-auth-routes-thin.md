2026-03-26：继续推进认证主链脱钩。新增 packages/engine/services/next-auth-bridge-service.ts，把 authOptions、NextAuth route handler、getServerSession(authOptions)、登录限流兼容响应统一收进单一 bridge service。packages/engine/auth.ts 已退成对 next-auth-bridge-service 的薄导出；packages/engine/services/auth-session-service.ts 已改成调用 readNextAuthSession()。src/app/api/auth/[...nextauth]/route.ts 现在只转发 handleNextAuthGetRequest / handleNextAuthPostRequest，路由层已清掉 NextAuth、限流、日志与兼容响应细节。另新增 packages/engine/services/auth-register-route-service.ts，把注册限流和注册响应下沉到 engine；src/app/api/auth/register/route.ts 已退成 apiHandler(handleRegisterUserRequest) 薄路由。npx tsc --noEmit --pretty false --incremental false 通过。