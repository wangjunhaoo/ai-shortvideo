2026-03-22：继续推进 renderer/novel-promotion 内部模块化。已新增 `packages/renderer/modules/project-detail/novel-promotion/components/script-view/hooks/useScriptViewAssetSelectionState.ts`，收口 ScriptViewAssetsPanel 的角色/场景弹层状态：showAddChar/showAddLoc、mounted、pendingAppearanceKeys/pendingAppearanceLabels、pendingLocationIds/pendingLocationLabels、saving 状态、变更检测、弹层开关、角色/场景确认保存。`ScriptViewAssetsPanel.tsx` 已完全改为通过 `selectionState.*` 读取与写入，不再在组件体内保留独立选择草稿状态。`npm run desktop:build:web` 已通过。后续可继续处理 `ScriptViewScriptPanel.tsx` 的内联编辑逻辑。