2026-03-22：继续推进 novel-promotion 剧集侧厚路由薄化。packages/engine/services/novel-promotion-project-service.ts 新增 createNovelPromotionEpisodesBatch，将 src/app/api/novel-promotion/[projectId]/episodes/batch/route.ts 中的项目查找、clearExisting 删除、空数组清空、批量创建、lastEpisodeId/importStatus 更新全部迁入 engine service。route.ts 现在只保留 requireProjectAuthLight、request.json、参数校验与 NextResponse 包装。真实回归没有单独再写新脚本，而是继续扩展 scripts/desktop/verify-update-routes.mjs：先通过 /episodes/batch 批量导入两集，再通过同一路由验证 clearExisting + 空数组清空分支，随后创建单集并继续验证 update-prompt。SQLite 回查确认：novel_promotion_projects.importStatus 已写回 cleared，清空后项目下只剩后续重建的单集，lastEpisodeId 正确指向新单集。