2026-03-22：继续推进 novel-promotion 轻量 CRUD/配置类路由的薄路由化。本轮新增 packages/engine/services/novel-promotion-voice-service.ts，将 voice-lines、speaker-voice、character-voice 三组路由中的台词 CRUD、发言人音色配置、角色音色设置与角色音频落库逻辑迁入 engine service。对应地，src/app/api/novel-promotion/[projectId]/voice-lines/route.ts、speaker-voice/route.ts、character-voice/route.ts 已收缩为认证、参数解析与 NextResponse 包装。桌面冒烟首次验证 character-voice 时暴露桌面模式本地存储绝对路径拼接错误：UPLOAD_DIR 在桌面模式下是绝对路径，但 src/lib/storage/providers/local.ts 与 src/app/api/files/[...path]/route.ts 仍用 path.join(process.cwd(), UPLOAD_DIR, ...)，在 Windows 下会生成 D:\code\...\C:\Users\... 的非法路径。已新增 src/lib/storage/utils.ts 的 resolveUploadRoot()，并更新 local provider 与 files route 统一兼容绝对上传根路径。此轮完成 npx tsc --noEmit 与 npm run desktop:build:web，并通过 desktop:run 真实接口冒烟验证 /api/novel-promotion/[projectId]/voice-lines、/api/novel-promotion/[projectId]/speaker-voice、/api/novel-promotion/[projectId]/character-voice；台词创建/查询/单条更新/批量音色更新/删除、发言人音色写入读取、角色音色设置更新、AI 设计音色保存、角色音频文件上传均正常。