2026-03-28：定位到桌面安装包批量生成视频时的 INVALID prisma.task.create() / SQLite socket timeout。根因是 packages/engine/services/novel-promotion-video-task-service.ts 在 batch 路径里使用 Promise.all 并发 submitTask，多 panel 同时落 task 表会在桌面 SQLite 单机环境下放大锁竞争；同类风险也存在于 novel-promotion-voice-service.ts 的批量语音提交。已新增 src/lib/task/submit-concurrency.ts，根据 DESKTOP_LOCAL_TASKS 或 SQLite DATABASE_URL 将批量提交并发降到 1；video/voice 两条 batch 提交链改用 mapWithConcurrency + resolveBatchTaskSubmitConcurrency，不再全量并发写 task。desktop/runtime/managed-web-runtime.cjs 组装桌面 SQLite DATABASE_URL 时追加 connection_limit=1 与 socket_timeout=30，进一步降低 Prisma SQLite 池竞争和短超时误判。已通过 tests/unit/novel-promotion/video-task-service.test.ts、tests/unit/task/submit-concurrency.test.ts 与 npm run desktop:build:web。