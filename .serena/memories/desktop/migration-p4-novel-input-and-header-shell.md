2026-03-22：继续推进 renderer/novel-promotion 的容器层瘦身。`NovelInputStage.tsx` 已拆出 `CurrentEpisodeBanner.tsx`、`NovelTextInputPanel.tsx`、`NovelInputConfigPanel.tsx`、`NovelInputActionPanel.tsx`，当前主要保留输入同步与顶层状态编排。`WorkspaceHeaderShell.tsx` 已拆出 `workspace-header/WorkspaceHeaderModals.tsx` 和 `workspace-header/WorkspaceEpisodeSelector.tsx`，设置/世界观弹窗与剧集选择器排序映射已移出，header 本体更接近导航与顶部动作壳。期间修正了 `NovelTextInputPanel` 的 composition 事件签名，以保持与 `useNovelInputTextState` 一致。`npm run desktop:build:web` 已通过。后续可继续压缩 `AssetsStage.tsx` 的 props 组合逻辑，或继续清理 header 壳与类型边界。