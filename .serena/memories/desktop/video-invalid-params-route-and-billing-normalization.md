2026-03-28：修复桌面端 generate-video 的同类 INVALID_PARAMS 问题，核心不是 provider，而是路由能力校验、计费和任务 payload 对视频参数的来源不一致。1) packages/core/model-capabilities/lookup.ts：视频能力解析前会丢弃 catalog 未声明的 generationMode（如 Veo 3.0/3.0 Fast），并结合 pricing tiers + normalizeVideoGenerationSelections 自动补齐合法的 duration/resolution 默认组合，因此运行时即使只给分辨率或空选择，也能收敛到合法值。2) packages/engine/services/novel-promotion-video-task-service.ts：submitNovelPromotionGenerateVideoTask 不再只做校验，改为先解析出标准化 generationOptions，再把这份结果回写到任务 payload 与 billingInfo，避免校验通过但 submitTask/worker 继续吃原始半成品参数。3) src/lib/billing/task-policy.ts：视频计费 metadata 不再无条件注入 generationMode，只在模型 capabilities 明确支持 generationModeOptions 时附带，避免 Veo 3.0 Fast 这类 normal-only 模型在 billing 校验阶段再次报 INVALID_PARAMS。4) 测试补充：tests/unit/generators/factory.test.ts 增加 Veo 3.0 Fast 默认值与 1080p->8s 组合回归；tests/unit/novel-promotion/video-task-service.test.ts 锁定标准化 generationOptions 会进入 task payload。