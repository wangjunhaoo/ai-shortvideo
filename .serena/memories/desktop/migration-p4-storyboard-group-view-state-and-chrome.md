2026-03-23：继续压缩 renderer storyboard 里的 StoryboardGroup.tsx。新增 hooks/useStoryboardGroupViewState.ts，把组级面板任务状态判断、运行中数量、待生成数量、overlay 状态以及清错后再生图逻辑从组件体抽出；新增 StoryboardGroupClipSection.tsx，把剧情折叠区和 screenplay/source text 视图独立出来；新增 StoryboardGroupChrome.tsx，把 failed alert、overlay、header、actions 组合成单独的顶栏壳层。这样 StoryboardGroup.tsx 现在更接近 group runtime + clip section + panel list + dialogs 的分发层。npm run desktop:build:web 已通过。下一步适合继续拆 hooks/usePanelCrudActions.ts 或 PanelVariantModal.tsx。