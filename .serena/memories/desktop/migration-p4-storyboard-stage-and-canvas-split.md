2026-03-23：回到 renderer 的 storyboard 容器层继续瘦身。新增 packages/renderer/modules/project-detail/novel-promotion/components/storyboard/StoryboardStageModals.tsx，把图片编辑、AI 数据、图片预览、角色/场景选择器等整块 modal 编排从 storyboard/index.tsx 抽出；新增 hooks/useStoryboardStageSectionProps.ts，把 StoryboardToolbar 和 StoryboardCanvas 的 props 组合逻辑从 index.tsx 收口到独立 hook，使 index.tsx 退回到 controller + modalRuntime + stage shell 的装配层。继续新增 StoryboardCanvas.types.ts 与 StoryboardCanvasItem.tsx，把 StoryboardCanvas.tsx 中单个分镜组项和“在此插入”按钮的列表项编排拆出，Canvas 现在主要保留空态和列表遍历。npm run desktop:build:web 已通过。下一步适合继续拆 StoryboardGroup.tsx 或 hooks/usePanelCrudActions.ts。