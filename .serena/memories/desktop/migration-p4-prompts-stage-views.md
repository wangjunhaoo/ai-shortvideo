2026-03-22：继续推进 renderer/novel-promotion 的 prompts-stage 容器瘦身。新增 `PromptListToolbar.tsx`、`PromptAppendSection.tsx`、`PromptStageNextButton.tsx`，把 `PromptListPanel.tsx` 中的列表头部/返回/批量生成/视图切换拼装，以及 `PromptEditorPanel.tsx` 中的追加输入区和下一步按钮分别拆成独立视图组件。当前 `PromptsStageLayout` 负责 runtime 装配，`PromptListPanel` / `PromptEditorPanel` 退回到容器分发层。`npm run desktop:build:web` 已通过。后续可继续清理其他 stage/container，或回头收缩 `AssetsStage` 中仍较长的 props 组合逻辑。