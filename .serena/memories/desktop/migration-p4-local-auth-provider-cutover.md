2026-03-26：认证主链已从 next-auth 运行时代码切到 local-auth-provider-service。auth-provider-service 现在统一承接 session/csrf/login/logout 与 [...nextauth] 兼容分发；next-auth-bridge-service 已改成废弃占位模块，不再 import next-auth 或 @next-auth/prisma-adapter。engine-auth-provider-boundary-guard 也已提升为禁止 packages/engine 与 src/app/api/auth 直接 import next-auth / next-auth bridge。当前 next-auth 仅剩 package.json/package-lock 中的遗留依赖元数据，可在获得依赖变更确认后再清理。