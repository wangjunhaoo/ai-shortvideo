2026-03-22：继续推进 renderer，`project-detail-page` 已进一步退化为装配层。新增 `useProjectDetailPageState.ts`，把 `project/episode` 查询、route state、model setup gate、episode actions、初始化判定与 `initLoadingState` 聚合到单一页面状态 hook。新增 `ProjectDetailStateViews.tsx`，承接 loading/error 页面壳。`packages/renderer/pages/project-detail-page.tsx` 现在只保留翻译、路由和分支渲染。顺手做了旧 `src/app/[locale]/workspace/[projectId]/modes/novel-promotion` 树的引用调查：外部入口已切到 `packages/renderer/modules/project-detail/novel-promotion/NovelPromotionWorkspace`，未再发现新的外部显式引用旧树；但旧树仍完整存在于 `src/app` 下，因此 Next 构建仍会编译它，构建 warning 里还会出现旧树文件。若要彻底清掉这部分，需要进入目录级删除旧树阶段，这属于高风险删除操作，需用户显式确认后执行。构建已通过。