2026-03-23：继续瘦身 renderer storyboard 的 hooks/usePanelCrudActions.ts。新增 usePanelSaveLifecycle.ts，把 PanelSaveCoordinator、saveState、savingPanels、debounce、retry、清理面板保存生命周期等逻辑整体抽出；新增 usePanelAssetEditActions.ts，把角色添加、角色移除、场景设置三类局部编辑动作抽出。这样 usePanelCrudActions.ts 现在主要负责接入 usePanelSaveLifecycle、处理新增/删除面板，以及汇总返回保存态与局部编辑动作。构建通过（使用重定向日志构建避免 EPIPE）。此步让 storyboard 这条链里的“生命周期协调”和“局部资产编辑”开始与 CRUD 业务动作分层。