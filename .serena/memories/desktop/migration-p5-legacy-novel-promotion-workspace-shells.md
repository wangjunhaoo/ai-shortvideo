2026-03-27：将旧 src/app/[locale]/workspace/[projectId]/modes/novel-promotion 的 workspace 级入口压成 renderer compat shell。已壳化 WorkspaceTopActions.tsx、WorkspaceStageContent.tsx、WorkspaceRunStreamConsoles.tsx、WorkspaceAssetLibraryModal.tsx、WorkspaceHeaderShell.tsx，以及 WorkspaceStageRuntimeContext.tsx。为通过旧树类型检查，补了 storyboard/index.tsx 的 picker labels，并让 PanelEditForm compat shell 转发 PanelEditData / CharacterPickerModalLabels / LocationPickerModalLabels 类型，同时把旧 storyboard 文件中的 PanelEditData 值导入改成 import type。新增 scripts/guards/legacy-novel-promotion-workspace-shell-guard.mjs，并接入 package.json 的 check:legacy-novel-promotion-workspace-shell 与 test:guards。desktop:build:web 与全部 legacy shell guards 已通过。