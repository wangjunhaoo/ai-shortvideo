2026-03-22：已把 runtime-context 与 repositories 正式迁入 packages/engine，并将 src/lib/workers/runtime-context.ts 与 repositories/*.ts 改成薄转发层，业务代码改走 @engine/*。同时已创建 packages/core，并完成第一批与第二批纯模块迁移：model-config-contract、storyboard-phases、prompt-i18n/*、model-capabilities/*、model-pricing/*。旧 src/lib 路径全部保留为薄转发层，packages/engine、src/lib/workers、src/app/api、src/app/[locale]、src/components 等引用已批量切到 @core/*。随后又把 packages/engine/config-service.ts 迁入 engine、把 packages/core/workflow-concurrency.ts 迁入 core，并将全仓库对 @/lib/config-service 与 @/lib/workflow-concurrency 的引用切到 @engine/* / @core/*。补充完成后再次通过 npx tsc --noEmit；本轮已通过 npx tsc --noEmit、npm run desktop:build:web，并对运行中的桌面实例执行 smoke-test，注册/登录/项目创建/项目列表/账单接口关闭均通过。