2026-03-27：desktop/runtime/page-module-dispatcher.cjs 在直调编译后的 app page handler 时缺少 Next 页面 request meta 和 next-intl locale header，导致访问 /zh 时在 next/dist/compiled/next-server/app-page.runtime.prod.js 中抛 `Cannot read properties of undefined (reading startsWith)`。修复方式：在 page dispatcher 中为原始 Node req/res 注入 originalRequest/originalResponse，并统一 addRequestMeta(initURL/initQuery/initProtocol/relativeProjectDir/distDir/middlewareInvoke/minimalMode/query/params/invokePath/invokeQuery/rewroteURL/locale/defaultLocale)，同时在匹配到动态 locale 段时补写请求头 x-next-intl-locale。验证结果：最小 HTTP server 直调 .next/server/app/[locale]/page.js，裸请求 500、补 meta 后 404、再补 x-next-intl-locale 后 /zh 返回 200；真实 engine-runtime-server 下 /api/system/boot-id 与 /zh 均返回 200。另更新 scripts/desktop/smoke-test.mjs，新增 verifyLandingPage(baseUrl, locale) 校验 HTML 响应和 <html lang="locale">，防止页面直出链回归。