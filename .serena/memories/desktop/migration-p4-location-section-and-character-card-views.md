2026-03-22：继续推进 renderer/novel-promotion 的 assets 区模块化。`CharacterCard.tsx` 已新增 `character-card/CharacterCardSelectionView.tsx` 与 `CharacterCardCompactView.tsx`，结合先前的 `useCharacterCardState.ts`，当前 CharacterCard 主要承担状态 hook 装配、动作节点拼装和 selection/compact 模式分发。`LocationSection.tsx` 已拆出 `location-section/LocationSectionHeader.tsx` 与 `LocationGridItem.tsx`，当前主要保留 `useProjectAssets(projectId)` 订阅和场景列表编排。`npm run desktop:build:web` 已通过。后续可继续拆 `LocationCard.tsx` 做角色/场景卡片结构对称，或处理 `ScriptViewRuntime.tsx`。