2026-03-22：继续推进 renderer/novel-promotion 的 header 容器瘦身。新增 `workspace-header/types.ts` 收口 `EpisodeSummary`、`UserModelsPayload`、`WorkspaceHeaderNavItem`、`WorkspaceHeaderShellProps`；新增 `WorkspaceHeaderControls.tsx` 承接 `CapsuleNav + WorkspaceTopActions` 装配。`WorkspaceHeaderShell.tsx` 已移除本地重复类型声明，并退回到 `WorkspaceHeaderModals`、`WorkspaceEpisodeSelector`、`WorkspaceHeaderControls` 三块分发壳。`WorkspaceHeaderModals.tsx` 与 `WorkspaceEpisodeSelector.tsx` 也已改为共用 `workspace-header/types.ts`。`npm run desktop:build:web` 已通过。后续可继续清理其他重复类型与 runtime/container 壳层，或继续拆 `novel-promotion` 下偏重组件。