2026-03-22：继续推进 renderer/novel-promotion 的 stage 容器拆分。新增 `packages/renderer/modules/project-detail/novel-promotion/components/assets/hooks/useAssetsStageViewState.ts`，把 `AssetsStage` 内的本地视图状态与派生值（`previewImage`、`toast`、`totalAppearances/totalLocations/totalAssets`、`getAppearances`、`onRefresh`）收口到轻量 hook；新增 `packages/renderer/modules/project-detail/novel-promotion/components/assets/AssetsStageSections.tsx`，把 `AssetsStageStatusOverlays`、`AssetToolbar`、`UnconfirmedProfilesSection`、`CharacterSection`、`LocationSection`、`AssetsStageModals` 的 JSX 拼装整体移出。`AssetsStage.tsx` 当前更接近稳定容器层，主要保留项目资产订阅、mutation/hook 装配和子组件 props 组合。`npm run desktop:build:web` 已通过。后续可继续拆 `PromptsStage.tsx` 或进一步抽 `AssetsStage` 的 props 组合逻辑。