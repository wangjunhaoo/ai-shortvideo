2026-03-22：继续推进单对象编辑路由的薄路由化。新增 packages/engine/services/novel-promotion-editing-service.ts，收口 updateNovelPromotionClip 与 updateNovelPromotionPanelLink。src/app/api/novel-promotion/[projectId]/clips/[clipId]/route.ts 与 panel-link/route.ts 已改为只保留鉴权、参数解析和 NextResponse 包装。此轮完成 npx tsc --noEmit 与 npm run desktop:build:web，并通过 desktop:run 的真实数据库做最小测试数据验证：先创建项目和剧集，再连接开发态桌面运行时使用的 SQLite（C:/Users/pc/AppData/Roaming/Electron/data/waoowaoo.db）插入 clip/storyboard/panel，然后验证 PATCH /api/novel-promotion/[projectId]/clips/[clipId] 与 POST /api/novel-promotion/[projectId]/panel-link，确认 clip 字段更新与 panel linkedToNextPanel 写回均正常。