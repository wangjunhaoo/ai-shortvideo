2026-03-22：继续推进 renderer 页面内部模块下沉，新增 `packages/renderer/modules/workspace`。首批已落文件：`types.ts`、`format-project-date.ts`、`WorkspaceSearchBar.tsx`、`WorkspaceProjectsSection.tsx`、`ProjectFormModal.tsx`、`WorkspacePagination.tsx`。`packages/renderer/pages/workspace-page.tsx` 已重构为页面编排层，只保留 `useSession`、路由跳转、项目列表请求、创建/编辑/删除状态与对话框状态；搜索栏、项目卡片区、空态、新建/编辑弹窗、分页 UI 改为从 `@renderer/modules/workspace/*` 引入。中途出现一处 `next-intl` translator 类型不兼容，已通过收紧模块 props 中 `t/tc` 的值类型为 `Record<string, string | number | Date>` 修复。`npm run desktop:build:web` 已再次通过，说明 workspace 页面第一轮模块化与当前 Next 外壳兼容。下一步适合继续抽 workspace 页面的数据/状态 hook，或回到 project-detail 继续清理更深的页面私有依赖。