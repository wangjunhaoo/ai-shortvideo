2026-03-26：继续推进 renderer/novel-promotion 模块级根容器翻译边界收口。新增 hooks/useNovelPromotionWorkspaceI18n.ts，把 NovelPromotionWorkspace.tsx 与 useNovelPromotionWorkspaceController.ts 共同使用的 novelPromotion/errors/common/progress 翻译组装统一抽到专用 hook；NovelPromotionWorkspace.tsx 已清掉直接 useTranslations(progress)，workspace 根容器的 run badge、run console、资产库按钮、refresh、toast 与 rebuild confirm 全改为消费显式 labels；useNovelPromotionWorkspaceController.ts 已清掉直接 useTranslations(novelPromotion/errors/common)，改为消费 useNovelPromotionWorkspaceI18n.ts 返回的 t/tc/te。另新增 useStageNavigationLabels.ts 并更新 StageNavigation.tsx，阶段导航按钮文案也已改成 labels hook。当前对 packages/renderer/modules/project-detail/novel-promotion 执行 rg -n "useTranslations\(" 后，只剩专用 i18n hook/labels hook 自身保留翻译边界，主运行链与展示组件已清零。npx tsc --noEmit --pretty false --incremental false 通过。