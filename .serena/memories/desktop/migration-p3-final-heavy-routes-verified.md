2026-03-22：最后三条重路由已完成 service 化并通过真实桌面回归。packages/engine/services/novel-promotion-project-service.ts 新增 splitNovelPromotionEpisodesByMarkers；packages/engine/services/novel-promotion-panel-variant-service.ts 接管 panel-variant 的校验、事务插入、失败回滚与 submitTask；packages/engine/services/novel-promotion-video-task-service.ts 接管 generate-video 的单 panel / 批量 all=true 分支、能力校验与计费构造。src/app/api/novel-promotion/[projectId]/episodes/split-by-markers/route.ts、panel-variant/route.ts、generate-video/route.ts 已全部改成薄路由。新增 scripts/desktop/verify-final-heavy-routes.mjs 与 npm 脚本 desktop:verify:final-heavy-routes。真实回归在桌面运行时自动完成注册登录、创建项目与三组剧集、SQLite 注入最小 clip/storyboard/panel 数据后，验证了 split-by-markers 成功返回 3 集，panel-variant 创建新 panel 且 graph_runs.input.newPanelId 正确，generate-video 单任务与批量任务都正确落库为 video_panel。随后用 rg -n "prisma\\.|submitTask\\(" src/app/api/novel-promotion -S 静态扫描已无命中，说明 novel-promotion 路由层的 prisma/submitTask 直连已清空。