2026-03-23：继续推进 renderer 入口与认证边界。新增 packages/renderer/auth/RendererSessionPendingScreen.tsx，把认证相关的等待态展示统一成共享组件，支持文本和 logo 两种模式。新增 packages/renderer/pages/landing-page.tsx，把首页入口也迁入 packages/renderer/pages；src/app/[locale]/page.tsx 已退成薄入口，只转发到 @renderer/pages/landing-page。packages/renderer/pages/profile-page.tsx、workspace-page.tsx、signin-page.tsx、signup-page.tsx 已改为复用 RendererSessionPendingScreen。结合前一轮的 useRequiredRendererSession/useGuestRendererSession 以及 signin/signup renderer 化，目前 renderer 认证入口的边界更完整：packages/renderer/auth 负责 provider/guard/pending screen，packages/renderer/pages 负责 landing/signin/signup/profile/workspace 页面入口，packages/renderer/modules/auth 负责认证表单状态与提交。npm run desktop:build:web 已通过。