2026-03-27：对 src/app/api 下剩余薄壳 route 做机械收口，统一把 NextRequest 注解替换成标准 Request，并删除 import { NextRequest } from 'next/server'。同时更新 src/lib/api-errors.ts，移除最后一个 NextRequest 类型依赖，apiHandler/getIdempotencyKey/extractRouteContext 全部改成基于 Request/Response + new URL(req.url)。使用 rg 校验，当前 src/app/api 中对 next/server 与 NextResponse 的命中均为 0；desktop:build:web、app-api-route-thinness-guard、desktop-runtime-adapter-registry-guard 均通过。