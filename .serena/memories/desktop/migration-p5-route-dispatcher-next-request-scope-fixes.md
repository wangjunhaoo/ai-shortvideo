2026-03-28：修复 packaged desktop smoke 中两层 Next App Router 运行时问题。第一层：/zh 页面在访问 /api/system/boot-id 后报 `Invariant: AsyncLocalStorage accessed in runtime where it is not available`，根因是 route-module-dispatcher 在 require 编译后的 app route 前没有先执行 next/dist/server/node-environment，导致 next/dist/server/app-render/async-local-storage 以缺失 AsyncLocalStorage 的状态被缓存。修复：desktop/runtime/route-module-dispatcher.cjs 在加载任意 route.js 前调用 desktop/runtime/next-page-runtime-support.cjs 的 ensureNextPageNodeEnvironment()。第二层：改走编译产物 handler 后，动态 API 路由 `/api/projects/[projectId]/costs` 报 `Cannot destructure property 'projectId' ... as it is undefined`，根因是 route prepare 链依赖 request meta 中的 params/query/rewroteURL。修复：在 next-page-runtime-support.cjs 新增 applyNextRouteRequestMeta()，由 route-module-dispatcher 在调用 compiled handler 前注入 initURL/initQuery/initProtocol/relativeProjectDir/distDir/middlewareInvoke/minimalMode/query/params/invokePath/invokeQuery/rewroteURL。验证：更新后的 win-unpacked 目录执行 `npm run desktop:smoke:packaged` 通过，链路覆盖 /zh、注册、登录、创建项目、项目列表、账单关闭校验。