2026-03-23：继续瘦身 renderer storyboard 的 ImageSectionActionButtons.tsx。新增 ImageSectionActionButtons.types.ts，统一收口图片区动作条的 props 契约；新增 hooks/useImageSectionActionButtonsState.ts，把候选图生成数量状态、重新生成点击日志和触发逻辑从动作条主体中抽出；新增 ImageSectionPrimaryActions.tsx，承接重新生成、查看 AI 数据、AI 编辑三类主动作区；新增 ImageSectionUndoAction.tsx，承接撤回上一版本按钮区与显示条件。这样 ImageSectionActionButtons.tsx 现在主要负责动作壳和主动作区/撤回动作的分发。desktop:build:web 已通过（日志构建在工具超时后继续完成，尾部已确认产物汇总）。