2026-03-23：继续推进 renderer 认证入口。新增 packages/renderer/pages/signin-page.tsx 与 signup-page.tsx，把登录页、注册页也迁入 packages/renderer/pages；新增 packages/renderer/modules/auth/useSignInForm.ts 与 useSignUpForm.ts，把认证表单状态、提交逻辑、错误/成功反馈从 src/app 页面中抽离。src/app/[locale]/auth/signin/page.tsx 与 signup/page.tsx 已退成薄入口，只转发到 @renderer/pages。结合同轮新增的 useRequiredRendererSession/useGuestRendererSession，目前 renderer 认证入口的基本边界已经形成：packages/renderer/auth 负责 session/provider 与 route guard，packages/renderer/pages 负责页面入口，packages/renderer/modules/auth 负责认证表单逻辑。npm run desktop:build:web 已通过。