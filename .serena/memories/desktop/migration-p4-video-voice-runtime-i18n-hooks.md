2026-03-26：继续推进 renderer/novel-promotion 的 video-stage / voice-stage / video 三条主链翻译边界收口。新增 components/video-stage/hooks/useVideoStageI18n.ts，把 VideoToolbar / VideoTimelinePanel / batch config modal labels 与 renderCapabilityLabel 提升到专用 i18n hook，useVideoStageRuntime.tsx 已清掉直接 useTranslations。新增 components/voice-stage/hooks/useVoiceStageI18n.ts，把 VoiceControlPanel / VoiceLineList / SpeakerVoiceBindingDialog labels、loading 文案、任务失败提示和 translate/translateWithValues 提升到专用 i18n hook，useVoiceStageRuntime.tsx 已清掉直接 useTranslations。新增 components/video/panel-card/runtime/useVideoPanelRuntimeI18n.ts，把 panel-card 展示链 labels 与 capability label formatter 从 videoPanelRuntimeCore.tsx 抽出，videoPanelRuntimeCore.tsx 已清掉 useTranslations(video/common)。另外补了两个未接主链的 video 候选组件：新增 useVideoPromptModalLabels.ts 与 useFirstLastFramePanelLabels.ts，VideoPromptModal.tsx 与 FirstLastFramePanel.tsx 也已切成 labels hook 模式。npx tsc --noEmit --pretty false --incremental false 通过；当前 video / video-stage / voice-stage 目录内仅剩专用 i18n hook 自身保留翻译边界。