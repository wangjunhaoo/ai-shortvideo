2026-03-22：继续推进 novel-promotion 资产侧路由薄化。packages/engine/services/novel-promotion-asset-service.ts 新增 createNovelPromotionCharacter，将角色创建、初始形象落库、参考图后台触发 reference-to-character 的逻辑从 src/app/api/novel-promotion/[projectId]/character/route.ts 的 POST 分支迁入 engine service。route.ts 现在只保留 requireProjectAuth、request.json、taskLocale/Accept-Language/Cookie 转发与 NextResponse 包装。随后复用 desktop:verify:update-routes 再跑一轮真实桌面回归，确认注册、登录、创建项目、创建角色、创建场景、update-appearance、update-location 全链正常，说明 character 创建重构未打断当前资产侧链路。