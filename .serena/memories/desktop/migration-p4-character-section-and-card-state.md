2026-03-22：继续推进 renderer/novel-promotion 的 assets 区模块化。`CharacterSection.tsx` 已拆出 `character-section/useFocusedCharacterScroll.ts`、`CharacterSectionHeader.tsx`、`CharacterGroupPanel.tsx`，当前主要保留 `useProjectAssets(projectId)` 订阅、计数计算与角色列表编排。`CharacterCard.tsx` 已新增 `character-card/useCharacterCardState.ts`，把 upload/delete menu/confirm selection/local pending/imageUrlsWithIndex/currentImageUrl/task presentation/upload pending 等本地状态与派生值下沉到 hook，组件本体更接近 selection/compact 两种模式的视图装配。`npm run desktop:build:web` 已通过。后续可继续拆 CharacterCard 的 JSX 模式壳，或对称处理 LocationSection。